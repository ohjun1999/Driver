<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Unchecked -->
    <item android:state_checked="false">
        <layer-list>
            <!-- 배경 -->

            <!-- 이미지 영역 -->
            <item android:drawable="@drawable/ic_maintain_state_false" android:gravity="center" />
        </layer-list>
    </item>
    <!-- Checked -->
    <item android:state_checked="true">
        <layer-list>
            <!-- 배경 -->

            <!-- 이미지 영역 -->
            <item android:drawable="@drawable/ic_maintain_state_true" />
        </layer-list>
    </item>
</selector>